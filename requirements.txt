# Runtime dependencies (kept in sync with pyproject.toml [project].dependencies).
#
# Needed by buildpack-based hosts that do not read pyproject.toml
# (e.g. the cloud.gov Python buildpack). Container hosts use the Dockerfile,
# which installs from pyproject.toml instead.
fastmcp>=2.0.0
httpx>=0.27.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
