# Runtime dependencies for the LOCAL MCP toolkit.
#
# watsonx Orchestrate installs a LOCAL MCP toolkit's deps from a requirements.txt
# at package_root (per the ADK docs). The register script stages this file next
# to a flattened copy of the `example_server` package so `python server.py`
# imports it directly (no build/install of the package itself needed) — only its
# third-party deps must be installed.
#
# Keep these in sync with the repo-root pyproject.toml / requirements.txt.
fastmcp>=2.0.0
httpx>=0.27.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
